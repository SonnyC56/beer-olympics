# Redis Configuration Example
# Copy this to your .env file and update with your Redis settings

# Redis Connection Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Optional: Redis Cluster or Sentinel Configuration
# REDIS_SENTINELS=sentinel1:26379,sentinel2:26379
# REDIS_SENTINEL_NAME=mymaster

# Optional: Redis Cloud or Managed Service
# For Redis Cloud/AWS ElastiCache/Azure Cache:
# REDIS_HOST=your-redis-instance.region.cache.amazonaws.com
# REDIS_PORT=6379
# REDIS_PASSWORD=your-auth-token

# Optional: Redis TLS/SSL Configuration
# REDIS_TLS=true
# REDIS_TLS_CA=/path/to/ca.pem
# REDIS_TLS_CERT=/path/to/cert.pem
# REDIS_TLS_KEY=/path/to/key.pem

# Cache TTL Overrides (in seconds)
# CACHE_TTL_LEADERBOARD=5
# CACHE_TTL_USER_SESSION=3600
# CACHE_TTL_TOURNAMENT=60
# CACHE_TTL_TEAM=120
# CACHE_TTL_DEFAULT=300

# Performance Tuning
# REDIS_MAX_RETRIES=3
# REDIS_CONNECT_TIMEOUT=10000
# REDIS_KEEPALIVE=30000